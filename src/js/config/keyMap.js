/**
 * 按键映射配置
 */
export const keyMap = {
  A: 0,
  S: 1,
  D: 2,
  J: 3,
  K: 4,
  L: 5, // 列0-5的键盘映射
};

// 可选：反向映射
// export const reverseKeyMap = Object.fromEntries(
//   Object.entries(keyMap).map(([key, value]) => [value, key])
// );
